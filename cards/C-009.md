# C-009 — Admin UI Redesign: Giao diện mới từ thiết kế Stitch

status: done
deps: ["C-008"]
implements: ["infra"]

## Scope

Redesign toàn bộ giao diện Admin Portal để bám sát thiết kế Stitch đã tạo (Soft Minimalism, Inter font, Deep Indigo/Soft Blue palette). Cụ thể:
1. Redesign `AdminSidebar` — sidebar trắng với active state indigo đậm (theo Stitch design).
2. Redesign `Admin Dashboard page.tsx` — stat cards hiện đại, chart placeholder/trend indicator, AI Provider status, Audit Log table.
3. Redesign `users/page.tsx` — table layout cao cấp, search bar nổi bật, tier badge gọn gàng.
4. Redesign `rag/page.tsx` — upload area & document table nhất quán với design system.
5. Redesign `checklists/page.tsx` — card list với count badge, hover state sạch.
6. Redesign `playground/page.tsx` — query input + results display sát với hệ thống.
7. Tạo file `admin.css` riêng cho admin system tách biệt với `fairterms.css` (không chạm code nghiệp vụ).

## Allowed files

- `src/frontend/app/admin/admin.css` (NEW)
- `src/frontend/app/admin/layout.tsx`
- `src/frontend/app/admin/page.tsx`
- `src/frontend/app/admin/components/AdminSidebar.tsx`
- `src/frontend/app/admin/users/page.tsx`
- `src/frontend/app/admin/rag/page.tsx`
- `src/frontend/app/admin/checklists/page.tsx`
- `src/frontend/app/admin/checklists/[id]/page.tsx`
- `src/frontend/app/admin/playground/page.tsx`
- `src/frontend/app/admin/ai-config/page.tsx`

## Verify (run these before calling the card done)

- [ ] `npm run lint` inside `src/frontend` — zero errors
- [ ] `npx tsc --noEmit` — zero TypeScript errors
- [ ] `AUTH_SECRET=dummy npm run build` — build succeeds, all admin routes render as Dynamic (ƒ)

## Done-evidence (world-state proof, named BEFORE building)

Build output shows all 7 `/admin/*` routes as `ƒ (Dynamic)` with zero compile errors.

## Evidence (paste the actual proof here when done)

Build output (2026-07-07):
- `✓ Compiled successfully in 2.4s` — 0 TypeScript/lint errors
- All 7 `/admin/*` routes show `ƒ (Dynamic)` in build manifest
- Dev server smoke test (NEXT_PUBLIC_MOCK_ADMIN=true): all 6 pages return HTTP 200
- New CSS design system created in `admin.css` (700+ lines, `adm-*` prefix, no conflicts with fairterms.css)
- All event handlers moved from Server Components to pure CSS hover classes
