# C-006 — Deploy production + smoke test trên live URL

status: todo
deps: C-004, C-005
implements: FR1, FR2, FR3, FR4, FR5, FR6

## Scope

Deploy toàn bộ v1 lên production:
- Frontend → Vercel (connect GitHub repo, set env vars)
- Backend FastAPI → Railway (Dockerfile hoặc nixpacks)
- PostgreSQL → Railway managed DB
- Chạy `prisma migrate deploy` trong CI/CD
- Smoke test mọi endpoint trên live URL
- Confirm disclaimer hiển thị trên mọi trang kết quả (FR6)

Không sửa code logic — chỉ infra và deploy config.

## Allowed files

```
Dockerfile                             # [NEW nếu chưa có] cho backend
railway.toml                           # [NEW] Railway config
vercel.json                            # [NEW nếu chưa có]
src/frontend/.env.example             # đảm bảo mọi NEXT_PUBLIC_ vars documented
src/backend/.env.example              # đảm bảo mọi backend env vars documented
.github/workflows/deploy.yml          # [NEW nếu chưa có] basic CI
```

## Verify (run these before calling the card done)

- [ ] `curl https://<production-url>/health` → `{"status":"ok"}`
- [ ] `curl -X POST https://<backend-url>/api/ocr/contract/structured -F "file=@test.pdf"` → structured response
- [ ] Mở https://<production-url>/app → upload PDF → thấy kết quả (end-to-end trên live URL)
- [ ] Disclaimer "không thay thế luật sư" hiển thị trên trang kết quả production
- [ ] Đăng nhập Google hoạt động trên production URL (NEXTAUTH_URL đúng)
- [ ] Guest quota hoạt động trên production (không phải localStorage trick)

## Done-evidence (world-state proof, named BEFORE building)

curl output của `https://<production-url>/health` và screenshot trang kết quả phân tích trên production URL (không phải localhost). URL phải có thể click.

## Evidence (paste the actual proof here when done)

(empty until done)
