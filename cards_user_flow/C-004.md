# C-004 — Auth (Google OAuth) + Guest session quota

status: todo
deps: C-001
implements: FR4

## Scope

Hoàn thiện: (1) Google OAuth login/logout flow hoạt động end-to-end trên production URL; (2) Guest session tracking: cookie-based UUID, đếm usage hôm nay, block khi vượt quota 3 phân tích/ngày; (3) UI: hiển thị trạng thái quota cho guest, prompt đăng nhập khi gần hết; (4) Sau đăng nhập: quota cao hơn (unlimited cho v1), lịch sử phân tích hiển thị.

## Allowed files

```
src/frontend/app/api/auth/[...nextauth]/route.ts
src/frontend/app/api/guest/session/route.ts     # [NEW] GET guest session status
src/frontend/app/api/guest/use/route.ts         # [NEW] POST consume 1 quota
src/frontend/lib/auth/guest-session.ts
src/frontend/app/fairterms/components/AuthHeader.tsx   # login/logout, quota display
src/frontend/app/fairterms/page.tsx                     # check quota before analyze
src/frontend/prisma/schema.prisma              # GuestSession, UsageEvent (nếu chưa có)
src/frontend/.env.example                      # đảm bảo AUTH_* vars documented
```

## Verify (run these before calling the card done)

- [ ] `curl http://localhost:3000/api/guest/session` (with cookie) → `{"guest_id":"...","used_today":0,"limit":3,"remaining":3}`
- [ ] Upload và phân tích 3 lần với guest → lần thứ 4 bị block với message tiếng Việt rõ ràng
- [ ] Bấm "Đăng nhập Google" → redirect về Google → sau auth, thấy avatar Google + tên user trong header
- [ ] Sau đăng nhập, phân tích không bị giới hạn quota
- [ ] `npx prisma db push` chạy không lỗi (schema sync)

## Done-evidence (world-state proof, named BEFORE building)

(1) Screenshot quota UI hiển thị "Còn 2/3 lần hôm nay" sau 1 lần phân tích guest. (2) Screenshot header sau khi đăng nhập Google, thấy tên/avatar. (3) Curl output `/api/guest/session`.

## Evidence (paste the actual proof here when done)

(empty until done)
