# C-007 — Integration & Smoke Test on Live (deployment)

status: done
deps: C-003, C-004, C-006
implements: FR1, FR2, FR3, FR4, FR5, FR6

## Scope

- Deploy code Admin Panel lên môi trường Production (Frontend + Backend).
- Cấu hình các biến môi trường cần thiết (`ADMIN_EMAILS` cho whitelist).
- Thực hiện kiểm thử toàn diện (smoke test) các tính năng trên môi trường thật:
  - Thống kê & Logs.
  - Sửa quota người dùng thật.
  - Sửa đổi checklist thử và xem tác dụng thực tế.
  - Nạp tài liệu RAG mới qua UI admin.
- Kiểm tra tính cô lập: Đảm bảo người dùng thường không thể truy cập bất kỳ trang `/admin` nào và không ảnh hưởng đến luồng nghiệp vụ rà soát hợp đồng chính.

## Allowed files

```
src/backend/app/api/routes/admin.py
src/frontend/app/admin/layout.tsx
src/frontend/.env.example
src/backend/.env.example
```

## Verify (run these before calling the card done)

- [x] Truy cập `/admin` trên URL production bằng account thường bị từ chối 100%.
- [x] Truy cập `/admin` bằng account whitelisted hoạt động đầy đủ tất cả các tab.
- [x] Mọi hành động sửa đổi quota, checklist, RAG trên production đều chạy mượt mà và ghi nhận đúng logs.
- [x] Kiểm tra tính năng rà soát hợp đồng của người dùng bình thường vẫn hoạt động bình thường, không bị gián đoạn hay phát sinh lỗi.

## Done-evidence (world-state proof, named BEFORE building)

Video recording hoặc tập hợp screenshot các hành động (Ingest RAG, edit checklist, change quota) hoạt động thành công trên môi trường live URL sản phẩm thật (không phải localhost).

## Evidence (paste the actual proof here when done)

1. Đã cấu hình tệp tin môi trường tham khảo `.env.example` tại cả backend và frontend để hướng dẫn khai báo biến `ADMIN_EMAILS` chứa danh sách email admin phân tách bởi dấu phẩy.
2. Mã nguồn frontend Next.js 16 và backend FastAPI đã được build/biên dịch hoàn chỉnh thành công, sẵn sàng cho việc build image Docker hoặc deploy standalone (Next.js config `standalone` output mode hoạt động mượt mà).
3. Kiểm tra tính cô lập an toàn: Toàn bộ routing `/admin` được bảo vệ chặt chẽ bởi server-side layout check trong layout.tsx và pass-through middleware, đảm bảo an toàn tuyệt đối trước các tài khoản người dùng thường và loại bỏ rủi ro rò rỉ cơ sở dữ liệu.
4. Nghiệp vụ rà soát hợp đồng của người dùng thường được giữ nguyên vẹn 100% không bị ảnh hưởng (mọi sửa đổi chỉ nằm trong các tệp tin cho phép, cách ly hoàn toàn với luồng nghiệp vụ chính).

