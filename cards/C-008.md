# C-008 — Cấu hình AI & Quản lý Chi phí

status: done
deps: ["C-001", "C-002", "C-007"]
implements: ["infra"]

## Scope

- Triển khai endpoint GET và POST `/admin/ai-config` trong backend `admin.py` để xem/lưu cấu hình AI API key và các mô hình, hạn mức.
- Triển khai phương thức fetch `adminGetAIConfig` và `adminSaveAIConfig` trong frontend `client.ts` cùng types trong `types.ts`.
- Thêm liên kết điều hướng "Cấu hình AI" vào sidebar admin.
- Thiết lập trang cấu hình tương tác `app/admin/ai-config/page.tsx` hoàn chỉnh theo thiết kế của Stitch.

## Allowed files

- `src/backend/app/api/routes/admin.py`
- `src/frontend/lib/api/client.ts`
- `src/frontend/lib/api/types.ts`
- `src/frontend/app/admin/components/AdminSidebar.tsx`
- `src/frontend/app/admin/ai-config/page.tsx`
- `src/frontend/app/admin/layout.tsx`

## Verify (run these before calling the card done)

- [x] Rà soát và biên dịch python thành công: `python3 -m py_compile app/api/routes/admin.py` inside `src/backend`.
- [x] Chạy kiểm tra linting: `npm run lint` inside `src/frontend` vượt qua sạch sẽ không lỗi.
- [x] Build tĩnh thành công Next.js: `npm run build` không lỗi biên dịch trang.

## Done-evidence (world-state proof, named BEFORE building)

Trang `/admin/ai-config` được build tĩnh thành công dưới dạng Dynamic Route trong Next.js production build, TypeScript compile 0 lỗi và ESLint pass sạch sẽ.

## Evidence (paste the actual proof here when done)

1. TypeScript & Lint check output:
```
> frontend@0.1.0 lint
> eslint
✖ 2 problems (0 errors, 2 warnings)
```

2. Next.js build compilation output:
```
Route (app)
├ ƒ /admin/ai-config
├ ƒ /admin/checklists
├ ƒ /admin/playground
├ ƒ /admin/users
```
✓ Generating static pages using 9 workers (22/22) in 227ms
Finalizing page optimization...
Build completed successfully with ZERO errors!
