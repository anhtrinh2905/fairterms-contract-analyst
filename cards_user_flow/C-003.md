# C-003 — Legal RAG: trích dẫn pháp luật cho điều khoản rủi ro

status: todo
deps: C-002
implements: FR3

## Scope

Với mỗi clause có risk_level "high" hoặc "medium", gọi `/rag/query` để lấy legal citations → hiển thị trong expandable section dưới clause card. Hiển thị: tên điều luật, số điều, đoạn trích văn bản pháp luật.

Không bao gồm: agent chat (đã có, không sửa), viewer `/van-ban/[docId]` (đã có).

## Allowed files

```
src/frontend/app/fairterms/components/ClauseCard.tsx   # thêm citations section
src/frontend/app/fairterms/components/LegalCitation.tsx  # [NEW] component hiển thị 1 citation
src/frontend/lib/api/client.ts                           # thêm ragQuery()
src/frontend/lib/api/types.ts                            # thêm Citation type
src/frontend/lib/api/mappers.ts                          # map citations
```

## Verify (run these before calling the card done)

- [ ] `curl -X POST http://127.0.0.1:8010/rag/query -H "Content-Type: application/json" -d '{"query":"điều khoản đơn phương chấm dứt hợp đồng mua bán nhà","top_k":3}'` trả về `citations` array không rỗng
- [ ] `curl http://127.0.0.1:8010/rag/status` → index không rỗng (có documents ingested)
- [ ] Trên frontend, click expand một clause rủi ro cao → thấy ít nhất 1 legal citation với tên luật và đoạn trích
- [ ] Citations hiển thị đúng tiếng Việt, không bị lỗi encoding

## Done-evidence (world-state proof, named BEFORE building)

Screenshot frontend: clause card với section citations mở rộng, hiển thị ít nhất 1 citation thực (tên luật + điều khoản + excerpt). Curl output của `/rag/query` với results thực.

## Evidence (paste the actual proof here when done)

(empty until done)
