# C-001 — Upload hợp đồng + OCR + hiển thị structured (vertical slice)

status: todo
deps: none
implements: FR1

## Scope

Vertical slice hoàn chỉnh: user upload PDF/ảnh → backend OCR (Gemini Vision) → trả về structured JSON (clauses list) → frontend hiển thị danh sách điều khoản.

Không bao gồm: phân tích rủi ro, RAG, share. Chỉ cần flow upload → xem nội dung hoạt động end-to-end.

## Allowed files

```
src/backend/app/api/routes/ocr.py
src/backend/app/services/ocr_service.py
src/backend/app/services/contract_structuring.py
src/frontend/app/fairterms/components/UploadArea.tsx
src/frontend/app/fairterms/components/ContractViewer.tsx
src/frontend/app/fairterms/page.tsx
src/frontend/lib/api/client.ts          # chỉ thêm uploadContract() nếu chưa có
src/frontend/lib/api/types.ts           # chỉ thêm types mới
```

## Verify (run these before calling the card done)

- [ ] `curl -X POST http://127.0.0.1:8010/api/ocr/contract/structured -F "file=@test_contract.pdf"` trả về JSON với `clauses` array có ít nhất 1 phần tử
- [ ] Mở http://localhost:3000/app, upload file PDF thử, thấy danh sách điều khoản xuất hiện trong < 60 giây
- [ ] Backend health check pass: `curl http://127.0.0.1:8010/health` → `{"status":"ok"}`
- [ ] Frontend không có lỗi console khi upload

## Done-evidence (world-state proof, named BEFORE building)

Screenshot màn hình frontend tại http://localhost:3000/app sau khi upload PDF, hiển thị ít nhất 3 điều khoản được trích xuất. Kèm curl output của `/api/ocr/contract/structured` với response JSON thực tế.

## Evidence (paste the actual proof here when done)

(empty until done)
