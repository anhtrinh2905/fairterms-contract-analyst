# C-002 — Phân tích rủi ro điều khoản + checklist UI

status: todo
deps: C-001
implements: FR2

## Scope

Sau khi C-001 hiển thị clauses, card này thêm: gọi `/checklist/evaluate-clause` cho từng điều khoản → hiển thị badge rủi ro (đỏ/vàng/xanh) + explanation_vi trên từng clause card. Thêm dropdown chọn loại hợp đồng trước khi phân tích. Thêm summary coverage từ `/checklist/evaluate-coverage`.

Không bao gồm: RAG citations (C-003), share (C-005).

## Allowed files

```
src/frontend/app/fairterms/components/ClauseCard.tsx
src/frontend/app/fairterms/components/RiskBadge.tsx
src/frontend/app/fairterms/components/ContractTypeSelector.tsx
src/frontend/app/fairterms/components/CoverageSummary.tsx
src/frontend/app/fairterms/page.tsx           # orchestrate analyze flow
src/frontend/lib/api/client.ts                # thêm evaluateClause(), evaluateCoverage()
src/frontend/lib/api/types.ts                 # thêm RiskLevel, EvaluationResult types
src/frontend/lib/api/mappers.ts               # map backend response → UI types
```

## Verify (run these before calling the card done)

- [ ] `curl -X POST http://127.0.0.1:8010/checklist/evaluate-clause -H "Content-Type: application/json" -d '{"clause_text":"Bên bán có quyền đơn phương chấm dứt hợp đồng mà không cần thông báo","loai_hop_dong":"hop_dong_mua_ban","checklist_item_id":"dieu_khoan_cham_dut"}' ` trả về `risk_level` là `"high"` hoặc `"medium"`
- [ ] Trên frontend, sau khi upload và chọn loại hợp đồng, bấm "Phân tích" → các clause hiển thị badge màu trong < 90 giây
- [ ] Ít nhất 1 clause có badge "Rủi ro cao" (đỏ) nếu upload hợp đồng có điều khoản bất lợi
- [ ] Coverage summary hiển thị phần trăm và danh sách điều khoản bắt buộc còn thiếu

## Done-evidence (world-state proof, named BEFORE building)

Screenshot frontend tại http://localhost:3000/app với ít nhất 3 clause cards hiển thị badge rủi ro màu khác nhau sau khi upload. Curl output của `/checklist/evaluate-clause` với response thực.

## Evidence (paste the actual proof here when done)

(empty until done)
