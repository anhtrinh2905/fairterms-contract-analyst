# C-005 — Share link kết quả phân tích

status: todo
deps: C-003
implements: FR5

## Scope

Sau khi phân tích xong (clauses + risk + citations), user bấm "Chia sẻ" → POST `/api/share` → nhận shareId → hiển thị link có thể copy. Trang `/share/[shareId]` public: hiển thị toàn bộ kết quả phân tích (read-only, không cần đăng nhập). Share chỉ khả dụng khi đã đăng nhập (không cho guest share để tránh lạm dụng).

## Allowed files

```
src/frontend/app/api/share/route.ts              # [NEW] POST /api/share
src/frontend/app/api/share/[shareId]/route.ts    # [NEW] GET /api/share/[shareId]
src/frontend/app/share/[shareId]/page.tsx        # [NEW] public share view page
src/frontend/app/fairterms/components/ShareButton.tsx  # [NEW]
src/frontend/app/fairterms/page.tsx              # thêm ShareButton sau analyze done
src/frontend/prisma/schema.prisma               # thêm SharedAnalysis model
src/frontend/lib/api/types.ts                   # thêm ShareResult type
```

## Verify (run these before calling the card done)

- [ ] Đăng nhập → phân tích hợp đồng → bấm "Chia sẻ" → nhận URL dạng `http://localhost:3000/share/abc123`
- [ ] Mở URL share trong private/incognito window (không đăng nhập) → thấy kết quả phân tích đầy đủ
- [ ] Share URL không expose nội dung hợp đồng gốc (chỉ hiển thị structured analysis)
- [ ] Guest user không thấy nút Share (hoặc bị disabled với tooltip "Đăng nhập để chia sẻ")

## Done-evidence (world-state proof, named BEFORE building)

Screenshot trang `/share/[shareId]` mở trong cửa sổ ẩn danh, hiển thị kết quả phân tích với clauses + risk badges. URL thực có thể click.

## Evidence (paste the actual proof here when done)

(empty until done)
