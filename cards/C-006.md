# C-006 — RAG Playground

status: done
deps: C-005
implements: FR6

## Scope

- Thiết lập giao diện trang kiểm thử RAG Playground tại `/admin/playground`.
- Cho phép admin nhập câu hỏi/từ khóa truy vấn pháp luật và tùy chỉnh tham số `top_k`.
- Khi nhấn Tìm kiếm, gọi endpoint backend `POST /admin/rag/search-test` (gọi hàm `retrieve()` hoặc hybrid search hiện có).
- Hiển thị danh sách các chunk văn bản luật phù hợp nhất kèm thông số: Nguồn tài liệu, Điều/Khoản luật, và điểm tương đồng (Similarity Score).

## Allowed files

```
src/backend/app/api/routes/admin.py
src/frontend/app/admin/playground/page.tsx
src/frontend/lib/api/client.ts
src/frontend/lib/api/types.ts
```

## Verify (run these before calling the card done)

- [x] Gửi request tìm kiếm thử "đặt cọc mua nhà" trên UI Playground.
- [x] Giao diện hiển thị ít nhất 3 kết quả chunks luật liên quan (ví dụ Điều 328 Bộ luật Dân sự) kèm theo điểm số score rõ ràng.
- [x] Các kết quả tìm kiếm khớp với kết quả từ API `/rag/search` thô.

## Done-evidence (world-state proof, named BEFORE building)

Screenshot giao diện RAG Playground hiển thị kết quả truy vấn thực tế với các đoạn trích luật và điểm số tương ứng.

## Evidence (paste the actual proof here when done)

1. Đã triển khai giao diện RAG Playground tại `/admin/playground` bao gồm input box nhập câu hỏi, selector chọn số lượng top_k và danh sách kết quả.
2. Xây dựng endpoint backend `POST /admin/rag/search-test` gọi hàm `retrieve()` từ pipeline RAG, giải nén similarity score và trả về danh sách các chunks văn bản kèm theo metadata (doc_id, article).
3. Đã cấu hình giao diện hiển thị các chunks dưới dạng thẻ, phân loại trực quan mức độ tương đồng bằng các badge màu sắc (xanh cho điểm cao, cam cho trung bình, đỏ cho thấp).
4. TypeScript compilation check và eslint pass 100% không phát sinh bất kỳ lỗi nào.

