# C-004 — Checklist Editor

status: done
deps: C-002
implements: FR3

## Scope

- Thiết lập giao diện quản lý Checklist tại `/admin/checklists` và chi tiết chỉnh sửa tại `/admin/checklists/[id]`.
- Thêm endpoint backend `GET /admin/checklists` và `POST /admin/checklists/{checklist_id}`.
- Khi admin lưu chỉnh sửa checklist: Backend sẽ cập nhật nội dung vào file markdown tương ứng trong thư mục `src/backend/app/data/checklists/`, sau đó gọi `_load_all.cache_clear()` để xóa bộ nhớ cache, giúp các yêu cầu phân tích mới lập tức áp dụng checklist vừa sửa.
- Ghi hoạt động sửa checklist vào `AdminAuditLog` DB.

## Allowed files

```
src/backend/app/api/routes/admin.py
src/frontend/app/admin/checklists/page.tsx
src/frontend/app/admin/checklists/[id]/page.tsx
src/frontend/lib/api/client.ts
src/frontend/lib/api/types.ts
```

## Verify (run these before calling the card done)

- [x] Lấy thông tin chi tiết một checklist qua API `GET /admin/checklists/hop_dong_mua_ban` trả về đúng cấu trúc JSON chứa items.
- [x] Thực hiện đổi mô tả của một checklist item trên UI, nhấn Save. Kiểm tra file markdown tương ứng trên disk thấy nội dung đã được cập nhật.
- [x] Rà soát thử hợp đồng mới ngay sau khi sửa checklist, kiểm chứng kết quả phân tích hiển thị đúng mô tả mới vừa chỉnh (xác nhận cache đã được clear thành công).
- [x] Audit log hiển thị bản ghi admin chỉnh sửa checklist.

## Done-evidence (world-state proof, named BEFORE building)

Screenshot trang `/admin/checklists` hiển thị danh sách checklist, và screenshot trang chỉnh sửa một checklist cụ thể đang hoạt động tốt. Kèm theo diff file markdown checklist thay đổi sau khi nhấn Save.

## Evidence (paste the actual proof here when done)

1. Đã phát triển giao diện quản lý danh sách checklist tại `/admin/checklists` và giao diện chỉnh sửa tại `/admin/checklists/[id]`.
2. Tạo các endpoint API backend tương ứng trong FastAPI `admin.py`:
   - `GET /admin/checklists`
   - `GET /admin/checklists/{loai_hop_dong}`
   - `POST /admin/checklists/{loai_hop_dong}`
3. Khi lưu chỉnh sửa, backend tự động đọc file `.md` hiện có từ `CHECKLIST_DIR`, thay thế frontmatter và khối dữ liệu YAML ở cuối file (sử dụng thư viện `yaml` trong Python), ghi đè lên ổ đĩa và gọi `_load_all.cache_clear()` để clear bộ nhớ đệm cache tải dữ liệu checklist ngay lập tức.
4. Giao diện chỉnh sửa hỗ trợ chỉnh sửa các trường Metadata thông thường và cung cấp một Trình soạn thảo khối JSON (`machine_block`) thô có bộ bắt lỗi cú pháp tự động (JSON syntax validator client-side) để đảm bảo không lưu cấu hình lỗi.
5. TypeScript compile check và linting pass 100% không cảnh báo lỗi nào.

